import { ScreenSpaceInputBindings, ScreenSpaceTiltOrbitCameraController as CesiumScreenSpaceTiltOrbitCameraController } from 'cesium';
import { PickCesiumProps } from '../core';
export type ScreenSpaceTiltOrbitCameraControllerCesiumProps = PickCesiumProps<CesiumScreenSpaceTiltOrbitCameraController, typeof cesiumProps>;
export type ScreenSpaceTiltOrbitCameraControllerCesiumReadonlyProps = {
    /**
     * Mouse button and optional keyboard modifier combinations that start a tilt or
     * orbit. Bindings are attached once when the controller is registered, so changing
     * this prop re-creates the controller rather than mutating it in place.
     */
    dragInputs?: ScreenSpaceInputBindings.InputBinding[];
};
export type ScreenSpaceTiltOrbitCameraControllerOtherProps = {
    /**
     * Precedence relative to other registered controllers. `0` applies before every
     * other controller; omitting it makes this controller the highest priority.
     * Changing this prop re-registers the controller.
     */
    priority?: number;
};
export type ScreenSpaceTiltOrbitCameraControllerProps = ScreenSpaceTiltOrbitCameraControllerCesiumProps & ScreenSpaceTiltOrbitCameraControllerCesiumReadonlyProps & ScreenSpaceTiltOrbitCameraControllerOtherProps;
declare const cesiumProps: readonly ["dampingEnabled", "enabled", "maximumMovementRatio", "maximumOrbitVelocity", "maximumTiltVelocity", "orbitAnimationDuration", "orbitEnabled", "orbitMagnitude", "pickWorldPosition", "tiltAnimationDuration", "tiltEnabled", "tiltMagnitude", "useDragPosition"];
declare const ScreenSpaceTiltOrbitCameraController: import('..').CesiumComponentType<CesiumScreenSpaceTiltOrbitCameraController, ScreenSpaceTiltOrbitCameraControllerProps>;
export default ScreenSpaceTiltOrbitCameraController;
