import { Camera } from 'cesium';
import { FC } from 'react';
export type CameraOperationProps = {
    cancelFlightOnUnmount?: boolean;
    once?: boolean;
};
export declare const createCameraOperation: <P>(name: string, cameraOperationStart: (camera: Camera, props: P, prevProps?: P) => void) => FC<P & CameraOperationProps>;
