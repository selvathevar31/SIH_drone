import { ImageryProvider } from 'cesium';
import { HTMLAttributes, ForwardRefExoticComponent, PropsWithoutRef, RefAttributes } from 'react';
import { Options } from './hooks';
export type CesiumComponentOptions<Element, Props extends RootComponentInternalProps, State = any> = Options<Element, Props, State> & {
    renderContainer?: boolean;
    noChildren?: boolean;
    containerProps?: (keyof Props)[] | ((props: Props) => HTMLAttributes<HTMLDivElement>);
    defaultProps?: Partial<Props>;
    /**
     * Render-phase hook to resolve prop overrides before the Cesium element is
     * created. Use this together with `useCesiumResource` to integrate async
     * resource loading with React Suspense. Returns a partial props object that
     * is merged over the incoming props.
     */
    useResource?: (props: Props) => Partial<Props> | undefined;
};
export type CesiumComponentRef<Element> = {
    cesiumElement?: Element;
};
export type CesiumComponentType<Element, Props> = ForwardRefExoticComponent<PropsWithoutRef<Props> & RefAttributes<CesiumComponentRef<Element>>>;
export type RootComponentInternalProps = {
    onUpdate?: () => void;
};
export type RootComponentInternalValues = {
    imageryLayerWaitingList?: (Promise<ImageryProvider> | ImageryProvider)[];
};
export declare const createCesiumComponent: <Element, Props extends object, State = any>({ renderContainer, noChildren, containerProps, defaultProps, useResource, ...options }: CesiumComponentOptions<Element, Props, State>) => CesiumComponentType<Element, Props>;
