import { ScreenSpaceInputBindings, ScreenSpaceElevatorCameraController as CesiumScreenSpaceElevatorCameraController } from 'cesium';
import { PickCesiumProps } from '../core';
export type ScreenSpaceElevatorCameraControllerCesiumProps = PickCesiumProps<CesiumScreenSpaceElevatorCameraController, typeof cesiumProps>;
export type ScreenSpaceElevatorCameraControllerCesiumReadonlyProps = {
    /**
     * Mouse button and optional keyboard modifier combinations that start a vertical pan.
     * Bindings are attached once when the controller is registered, so changing this
     * prop re-creates the controller rather than mutating it in place.
     */
    dragInputs?: ScreenSpaceInputBindings.InputBinding[];
};
export type ScreenSpaceElevatorCameraControllerOtherProps = {
    /**
     * Precedence relative to other registered controllers. `0` applies before every
     * other controller; omitting it makes this controller the highest priority.
     * Changing this prop re-registers the controller.
     */
    priority?: number;
};
export type ScreenSpaceElevatorCameraControllerProps = ScreenSpaceElevatorCameraControllerCesiumProps & ScreenSpaceElevatorCameraControllerCesiumReadonlyProps & ScreenSpaceElevatorCameraControllerOtherProps;
declare const cesiumProps: readonly ["enabled", "inertiaEnabled", "inertialDecay", "maximumMovementRatio", "panSpeed", "pickWorldPosition"];
declare const ScreenSpaceElevatorCameraController: import('..').CesiumComponentType<CesiumScreenSpaceElevatorCameraController, ScreenSpaceElevatorCameraControllerProps>;
export default ScreenSpaceElevatorCameraController;
