import { Camera } from 'cesium';
type Options = Parameters<Camera["flyTo"]>[0];
export type CameraFlyToProps = Omit<Options, "complete" | "cancel"> & {
    onComplete?: Options["complete"];
    onCancel?: Options["cancel"];
    /** If true, cancel camera flight if this component is unmounted. Default value is false. */
    cancelFlightOnUnmount?: boolean;
    /** If true, camera flight will be executed only once time. */
    once?: boolean;
};
declare const CameraFlyTo: import('react').FC<Omit<{
    destination: import('cesium').Cartesian3 | import('cesium').Rectangle;
    orientation?: any;
    duration?: number;
    complete?: Camera.FlightCompleteCallback;
    cancel?: Camera.FlightCancelledCallback;
    endTransform?: import('cesium').Matrix4;
    maximumHeight?: number;
    pitchAdjustHeight?: number;
    flyOverLongitude?: number;
    flyOverLongitudeWeight?: number;
    convert?: boolean;
    easingFunction?: import("cesium").EasingFunction.Callback;
}, "complete" | "cancel"> & {
    onComplete?: Options["complete"];
    onCancel?: Options["cancel"];
    /** If true, cancel camera flight if this component is unmounted. Default value is false. */
    cancelFlightOnUnmount?: boolean;
    /** If true, camera flight will be executed only once time. */
    once?: boolean;
} & import('..').CameraOperationProps>;
export default CameraFlyTo;
